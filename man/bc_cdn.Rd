% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdn.R
\name{bc_cdn}
\alias{bc_cdn}
\title{Basecoat from a CDN}
\usage{
bc_cdn(style = "vega", js = TRUE, version = bc_version)
}
\arguments{
\item{style}{String. A style pack: one of vega, nova, maia, lyra, mira, luma, sera, rhea. Each is a
standalone bundle, so a page loads exactly one.}

\item{js}{\code{TRUE} for every component's script, \code{FALSE} for none, or a
character vector of component names to load beside the runtime.}

\item{version}{String. The \code{basecoat-css} release to serve.}
}
\value{
An \code{\link[htmltools:htmlDependency]{htmltools::htmlDependency()}}.
}
\description{
The Basecoat stylesheet and scripts as an \code{\link[htmltools:htmlDependency]{htmltools::htmlDependency()}},
served from jsDelivr and placed in the page \verb{<head>}.
}
\details{
Attach it with \code{\link[htmltools:attachDependencies]{htmltools::attachDependencies()}}, or return it in a
\code{\link[htmltools:tagList]{htmltools::tagList()}} beside your markup. Call it once per page: a second
call with another style is de-duplicated by name and only one wins.

Basecoat is authored for Tailwind. Load any other Tailwind build before this
dependency, never after, or that build resets borders and inputs to their
own defaults.
}
\examples{
bc_cdn()

bc_cdn(style = "maia", js = c("select", "toast"))
}
